import { ImageRolloverDirective } from './image-rollover.directive';

describe('ImageRolloverDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageRolloverDirective();
    expect(directive).toBeTruthy();
  });
});
