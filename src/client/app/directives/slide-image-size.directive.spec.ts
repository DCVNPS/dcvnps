import { SlideImageSizeDirective } from './slide-image-size.directive';

describe('SlideImageSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new SlideImageSizeDirective();
    expect(directive).toBeTruthy();
  });
});
