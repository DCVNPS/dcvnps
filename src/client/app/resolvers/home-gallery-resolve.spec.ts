import { HomeGalleryResolve } from './home-gallery-resolve';

describe('HomeGalleryResolve', () => {
  it('should create an instance', () => {
    expect(new HomeGalleryResolve()).toBeTruthy();
  });
});
